# Kettle UI API

This module provides interfaces for common UI widgets used in Pentaho dialogs and configuration screens.

## Usage

These interfaces are used to build dialogs such as `RunConfigurationDialog`, which assembles widgets like text fields, groups, and buttons to create interactive configuration screens.

Refer to dialog implementations for practical usage examples.
